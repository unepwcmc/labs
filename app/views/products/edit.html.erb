<div class="page-header">
  <h1>Editing Product</h1>
</div>

<div class="btn-group">
  <%= button_to 'Back', products_path, method: :get, :class => 'btn btn-primary' %>
  <%= button_to 'Show', @product, method: :get, :class => 'btn btn-primary' %>
</div>

<%= render 'form' %>
