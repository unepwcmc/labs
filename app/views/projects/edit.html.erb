<div class="page-header">
  <h1>Editing project</h1>
</div>

<div class="btn-group">
  <%= button_to 'Back', projects_path, method: :get, :class => 'btn' %>
  <%= button_to 'Show', @project, method: :get, :class => 'btn primary' %>
</div>

<%= render 'form' %>
