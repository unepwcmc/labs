<%= form_for(@dependency, :html => {class: "form-horizontal"}) do |f| %>
  <% if @dependency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependency.errors.count, "error") %> prohibited this dependency from being saved:</h2>

      <ul>
        <% @dependency.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :sub_project_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.collection_select :sub_project_id, Project.order(:title),:id,:title,
        {prompt: "Select the dependant project"}, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :master_project_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.collection_select :master_project_id, Project.order(:title),:id,:title,
        {prompt: "Select the master project"}, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>

<% end %>